apiVersion: v1
kind: Service
metadata:
  name: helloworld-service
spec:
  selector:
    type: simple-app
  type: NodePort
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30081
      protocol: TCP

